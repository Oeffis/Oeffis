/**
 * Enables something (e.g. a search result) to have a rating describing the quality of the result.
 */
export interface Rateable {

  /** Some number that is representing a rating; higher means better. */
  rating: number;

}
