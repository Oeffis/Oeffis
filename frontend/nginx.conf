events {
    worker_connections 1024;
}

http {
    include mime.types;

    server {
        listen 80;
        root /app/www;

        location / {
            set $ssiApiUrl "${API_URL}";
            root /app/www;
            index index.html;

            try_files $uri /index.html;
        }
    }
}
